import React from 'react';

// Phase 2.7 — Horizontal log-frequency bar with 4 draggable crossover points
export default function CrossoverEditor({ freqs, bands, onChange }) {
  // TODO: Implement draggable crossover frequency editor (Phase 5.5 / Phase 9)
  return (
    <div style={{ width: '100%', height: 30 }}>
      {/* Placeholder — implement in Phase 9 */}
    </div>
  );
}
